#include <gtest/gtest.h>
#include "Player.hpp"
/*
struct PlayerTests : public::testing::Test
{
    Player player{'X'};
};

TEST_F(PlayerTests, playerCreatedProperly)
{
    ASSERT_FALSE(player.getTurn());
    ASSERT_EQ('X',player.getSign());

    player.setTurn(!player.getTurn());
    ASSERT_TRUE(player.getTurn());
}
*/
